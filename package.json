{
  "name": "roum-token",
  "version": "1.0.0",
  "description": "ROUM Token repo - dev configuration for CI & tests",
  "scripts": {
    "test": "npx hardhat test",
    "lint:sol": "npx solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "solhint": "^3.3.7",
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
